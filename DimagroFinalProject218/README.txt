To store rsuers i used a binary search. When signed up all the users went into a Treemap with thier username paired.
The posts are stored in a linked list to keep the order of the post
the dictionary is stored in a hastset so there can be no duplicates and efficent searching for the words
